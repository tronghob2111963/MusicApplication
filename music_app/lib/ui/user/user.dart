import 'package:flutter/material.dart';

class acountTab extends StatelessWidget {
  const acountTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'User Tab',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
